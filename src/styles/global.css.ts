import { globalStyle } from "@vanilla-extract/css";

globalStyle("html, body", {
  height: "100vh",
  width: "100vw",
  margin: 0,
  padding: 0,
});

globalStyle("h1, h2, h3, h4, h5, h6", {
  margin: 0,
  padding: 0,
});
